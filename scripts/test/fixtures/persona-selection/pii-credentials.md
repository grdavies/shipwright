# Fixture: PII/credentials extension (security-tagged)

## Summary

Store user credentials with encryption at rest.

## Requirements

- R1: Encrypt PII fields before persistence.

<!-- expected-personas: core + security (matched: credentials or PII or encryption) -->
