# Fixture: unambiguous UI term fires design gate

## Summary

Redesign checkout navigation for responsive layouts.

## Requirements

- R1: Update primary `navigation` with `accessibility` improvements.

<!-- expected-personas: core + design (matched: navigation or accessibility) -->
