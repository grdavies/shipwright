# Gap backlog (living, append-only)

Committed trivial in-scope gaps written out-of-loop by `/sw-feedback` (Phase 2). Unlike frozen task
lists, this file is hand-appendable and not subject to the freeze CI check.

## Index

Stable IDs (`GAP-NNN`) are assigned at append time — **never reuse**. Cross-link gaps as `GAP-NNN`, not
table row numbers. When appending, use the next ID: **`GAP-047`**.

| Status | Count |
|--------|------:|
| resolved | 16 |
| partially resolved | 5 |
| planned | 14 |
| open | 11 |

| ID | Status | Title |
|----|--------|-------|
| GAP-001 | resolved | Pre-push secret scan: `sk_live_` fixture string reached `git push` and w |
| GAP-002 | resolved | Scoped history-redaction guardrail: removing a secret from history must  |
| GAP-003 | resolved | Phase `status.json` written under the phase worktree's `.cursor/` but `w |
| GAP-004 | resolved | `merge run-next` assumes a per-phase PR (`gate-check` → "no open PR"); l |
| GAP-005 | resolved | Orchestrator merges into its own worktree checkout; the primary checkout |
| GAP-006 | resolved | `core/scripts/` mirror drift: |
| GAP-007 | resolved | `doc.afterTasks` guidance stale in `sw-doc.md`: |
| GAP-008 | resolved | `doc.afterTasks: confirm` checkpoint easy to miss: |
| GAP-009 | partially resolved | Model tier routing not bound at Task dispatch (R9 procedural only): |
| GAP-010 | resolved | CI link-check automation (v1 non-goal): |
| GAP-011 | open | PRD 003 not started — deferred v2 surface entirely open: |
| GAP-012 | planned | `/sw-deliver` v1 deferrals still open: |
| GAP-013 | open | Native review panel follow-on deferrals (post-PR #61): |
| GAP-014 | partially resolved | Model tier config deferrals (distinct from runtime R9 gap): |
| GAP-015 | resolved | `/sw-cleanup` confirm step is a manual bash hand-off, not an agent-drive |
| GAP-016 | planned | Frozen PRDs are not auto-committed to `main`, creating a data-loss windo |
| GAP-017 | planned | `deliver-loop` supports only one concurrent PRD; parallel product-area w |
| GAP-018 | resolved | Provider-conditional source of truth (R32 / KTD3 revisit): |
| GAP-019 | resolved | `/sw-compound` naming does not match operator mental model: |
| GAP-020 | resolved | `/sw-compound` + `/sw-compound-ship` overlap looks like an oversight: |
| GAP-021 | partially resolved | Retrospective/compounding not auto-run before terminal PR in `/sw-delive |
| GAP-022 | planned | Terminal PR + CI watch + stabilize should run autonomously; merge to `ma |
| GAP-023 | resolved | Manual PR test plans are not enforced by CI — `/sw-watch-ci` cannot catc |
| GAP-024 | resolved | `/sw-compound-ship` still has human approval gates that block full auton |
| GAP-025 | planned | `/sw-cleanup` still requires explicit human confirm before apply: |
| GAP-026 | planned |  during PRD 012 phase-mode deliver, the agent halted with `Resume: bash  |
| GAP-027 | planned | Conductor paused after post-remediation green-merged phase ("status-paus |
| GAP-028 | planned | `compoundShip` / `record-premerge` writes orchestrator worktree state bu |
| GAP-029 | planned | Conductor paused mid-phase during `dispatch-ship` (incomplete ship work  |
| GAP-030 | planned | Phase worktrees not torn down immediately after `green-merged` — linger  |
| GAP-031 | planned | Pre-work memory search not a categorical, enforced obligation (consisten |
| GAP-032 | open | `copy-to-core.sh` destructive sync vs split source-of-truth (investigate |
| GAP-033 | resolved | `/sw-cleanup` apply order deletes branches before worktrees — predictabl |
| GAP-034 | partially resolved |  `merged_status()` in `cleanup_lib.py` classifies branches via ancestor- |
| GAP-035 | open | Phase-mode `/sw-ship` opens orphan PR to `main` — deliver merges locally |
| GAP-036 | planned | No review persona ensures documentation artifacts affected by a change a |
| GAP-037 | open | Dangling phase-mode PRs recur — TWO distinct non-close modes confirmed ( |
| GAP-038 | open | No spec-mutation safety when a PRD/task list is being implemented in ano |
| GAP-039 | planned |  `core/hooks/before_task_dispatch.py` validates bound reviewer / native- |
| GAP-040 | planned | `resolve-model-tier.sh` returns different tiers for `--command` vs `--ag |
| GAP-041 | open | Terminal `retrospective` pause after all waves — `all_phases_merged` vs  |
| GAP-042 | open | Phase `status.json` path edge case recurs — background dispatch + relati |
| GAP-043 | open | Backlog status feedback loop not mechanical — `planned` rows stay stale  |
| GAP-044 | partially resolved | Stable gap IDs + index for cross-reference (manual contract): |
| GAP-045 | open | Model-dependent doc formatting breaks PRD/amendment/task automation — strict regex parsers vs free-form generation; parsers also disagree |
| GAP-046 | open | Gap absorption into a PRD/amendment does not mechanically flip status (open → planned) — upstream half of GAP-043 |

## Table

| ID | Date | Source | PRD | Gap | Status |
|----|------|--------|-----|-----|--------|
| GAP-001 | 2026-06-25 | /sw-deliver PRD 005 run | 004 | Pre-push secret scan: `sk_live_` fixture string reached `git push` and was caught only by GitHub push protection (last line, not first). Add a deny-pattern scan (`sk_(live\\|test)_`, `ghp_`, PEM, etc.) to `/sw-stabilize` or terminal-PR prep so secrets fail locally before push. | resolved — PRD 007 R41/R50/R51 (`scripts/secret-scan.sh`), PR #67 |
| GAP-002 | 2026-06-25 | /sw-deliver PRD 005 run | 004 | Scoped history-redaction guardrail: removing a secret from history must be range-scoped (`git filter-branch <base>..<branch>` or `git rebase -i`); a bare-branch `filter-branch` rewrote shared main commits under new SHAs and produced spurious PR conflicts. Add a `rules/` guardrail + doc note. | resolved — PRD 007 R42/R52 (`scripts/redaction-guard.sh` + `core/rules/sw-redaction-scope.mdc`), PR #67 |
| GAP-003 | 2026-06-25 | /sw-deliver PRD 005 run | 004 | Phase `status.json` written under the phase worktree's `.cursor/` but `wave.sh status collect` reads the orchestrator-root path; required manual copy per phase. `status collect` should resolve the durable phase-worktree path directly (R38 intent). | resolved — PRD 007 R38, PR #67 |
| GAP-004 | 2026-06-25 | /sw-deliver PRD 005 run | 004 | `merge run-next` assumes a per-phase PR (`gate-check` → "no open PR"); local phase-mode has no PRs, forcing manual `merge exec` fallback every phase. Add a no-PR local-merge path for phase-mode. | resolved — PRD 007 R39/R54, PR #67 |
| GAP-005 | 2026-06-25 | /sw-deliver PRD 005 run | 004 | Orchestrator merges into its own worktree checkout; the primary checkout's `<type>/<slug>` ref needed manual `git merge --ff-only` after each phase. Deliver should advance/sync the primary ref automatically. | resolved — PRD 007 R40/R55, PR #67 |
| GAP-006 | 2026-06-25 | PRD 009 deliver review (604abd4 revert) | 009 | **`core/scripts/` mirror drift:** runtime + fixtures use repo-root `scripts/` (`wave.sh` → `$ROOT/scripts/*`). `core/scripts/` is generated by `scripts/copy-to-core.sh` (PRD 004 — MUST NOT hand-edit). After PRD 009 phases the mirror is stale (~15 divergent files; missing `docs-presence-check.sh`, `docs-currency-gate.sh` hooks in `wave.sh`, etc.). Premature commit `604abd4` patched `core/scripts/` directly and deleted `model-routing.defaults.json`. **Follow-up:** run `bash scripts/copy-to-core.sh`, commit sync, and add a parity fixture (or wire into `run-emitter-fixtures.sh` / `verify.test`) so `core/scripts/` cannot drift from `scripts/` undetected. | resolved — `scripts/test/run-core-scripts-parity-fixtures.sh` + `copy-to-core.sh` sw-reference preserve (PRD 009 follow-up) |
| GAP-007 | 2026-06-25 | /sw-doc PRD 003 run + user review | 005 | **`doc.afterTasks` guidance stale in `sw-doc.md`:** frozen amendments A1/A2 require `/sw-deliver run <frozen-task-list>` as the user-facing post-freeze entry (with `wave.sh spec-seed` on the boundary for `stop`/`confirm`/`auto`), but `core/commands/sw-doc.md` still prints raw `bash scripts/wave.sh deliver-loop` as the primary next command. **Follow-up:** align `sw-doc.md` (and dist emit) with A1 R76–R79 / A2 R80–R83; ensure `doc-afterTasks-*` fixtures pass; user guides already document `/sw-deliver run`. | resolved — PRD 011 R1–R4 (PR #76) |
| GAP-008 | 2026-06-25 | /sw-doc PRD 003 run + user review | 002 | **`doc.afterTasks: confirm` checkpoint easy to miss:** with `doc.afterTasks: confirm`, `/sw-doc` emits the proceed/yes ask as prose at the end of a long pipeline summary (soft halt — no blocking UI). Unrelated follow-up messages (e.g. `/sw-memory-sync`, doc questions) do not re-prompt and correctly map to stop, but users may not realize implementation is waiting on an explicit ack. **Follow-up:** make the confirm boundary visually prominent in `sw-doc.md` output contract (dedicated section, direct question, halt without unrelated work); consider re-emitting checkpoint when user returns without ack; optional fixture for confirm prompt shape per PRD 002 R2. | resolved — PRD 011 R5–R7 (PR #76) |
| GAP-009 | 2026-06-25 | /sw-doc PRD 010 run + user review | 008 | **Model tier routing not bound at Task dispatch (R9 procedural only):** `models.routing` maps `sw-doc-review` → `build` → `composer-2.5`, but persona subagents (`sw-*-reviewer`, `model: inherit`) ran on Haiku 4.5 because Cursor resolves `inherit` from the parent chat session — not `workflow.config.json`. PRD 008 scoped out hook-based Task injection and mandatory `resolve-model-tier.sh` at dispatch time; static CI cannot catch runtime drift. **Should close:** yes — tier policy is meaningless if subagents ignore it. **Can close (partial → full):** (1) **procedural** — harden `/sw-doc-review` + `sw-subagent-dispatch` to require `resolve-model-tier.sh --command sw-doc-review` and explicit Task `model:` from `models.tiers` before every persona spawn; R9 halt when parent &lt; `roles.builder`; fixtures for dispatch contract; (2) **mechanical** — explore Cursor pre-tool hook to inject `modelId` on Task calls targeting `sw-*-reviewer` / native panel agents (PRD 008 deferred); (3) **structural** — replace reviewer `model: inherit` with setup-stamped concrete IDs or platform-default `build` slug (loses per-repo tier edits unless re-seeded). Full closure likely needs (1)+(2). | partially resolved — PRD 012 R1–R5 shipped (PR #79); residual runtime gaps GAP-039, GAP-040 |
| GAP-010 | 2026-06-25 | PRD 001–008 deferred-item audit | 001 | **CI link-check automation (v1 non-goal):** PRD 001 deferred automated link verification; R16 still requires manual link checks for README + guides. **Follow-up:** add optional `scripts/docs-link-check.sh` (or lychee/markdown-link-check) wired into `verify.test` / a doctor step — not blocking ship, but closes the v1 manual-only debt. | resolved — PRD 011 R11–R13 (PR #76) |
| GAP-011 | 2026-06-25 | PRD 001–008 deferred-item audit | 003 | **PRD 003 not started — deferred v2 surface entirely open:** INDEX `not-started`. Parent + A1 deferred items still unimplemented: `review.provider: pr-agent` adapter + `/sw-setup` workflow scaffold; PR-Agent CLI in `/sw-review` phase 2 (DL-13 — upstream uncommitted review); `issue_comment` interactive tools; `auto_improve` in scaffolded workflow; in-repo `.pr_agent.toml` / `configuration.toml` deep customization; org-level GitHub Environments / OIDC / `/sw-upstream` pin automation. PRD 005 native panel shipped as the phase-1 local path instead. | open — reviewed 2026-06-26 (GAP triage): this IS PRD 003's own frozen-but-not-started deferred scope; tracked by PRD 003, no new PRD warranted |
| GAP-012 | 2026-06-25 | PRD 001–008 deferred-item audit | 004 | **`/sw-deliver` v1 deferrals still open:** (1) cross-feature waves mixing phase-mode + multi-feature units in one plan; (2) automatic file-set edge inference when phase metadata is missing (sequential fallback only today); (3) rich live `living-status` per-phase dashboard (minimal `run.log` + terminal report shipped — R54); (4) contention serialization durable feedback into `/sw-tasks` re-run (DL-14 — runtime notice only). **Subsequently closed elsewhere (no gap):** `doc.afterTasks` → `/sw-deliver` dispatch (PRD 005 A1, PRD 007 R5); Owner A `spec-seed` at `deliver-loop` entry (`wave_deliver_loop.py`); shared branch resolver via `wave.sh preflight` + `wave_spec_seed.py`. | planned — PRD 013 R13–R16 (frozen) |
| GAP-013 | 2026-06-25 | PRD 001–008 deferred-item audit | 005 | **Native review panel follow-on deferrals (post-PR #61):** PRD marked complete; explicit deferrals remain: (1) hybrid native-shell + pluggable-engine architecture (DL-2 — `ce-code-review` selectable only); (2) adaptive per-repo latency budgeting / auto-skip-low-risk heuristics (`--fast`/`--skip-local` escape hatch shipped per R54/DL-19); (3) stack-specific specialists (frontend-races, iOS, etc.) on evidence. | open — reviewed 2026-06-26 (GAP triage): evidence-gated/speculative; revisit when usage evidence exists, no PRD authored yet |
| GAP-014 | 2026-06-25 | PRD 001–008 deferred-item audit | 008 | **Model tier config deferrals (distinct from runtime R9 gap):** PRD 008 non-goals still open: (1) `models.platforms.*` dual-map in one config (Option B — rejected D1, deferred revisit); (2) `models.routing.agents` map — per-agent tiers remain procedural in `sw-subagent-dispatch.mdc` only; (3) user-interactive model picker in `/sw-setup` (defaults + manual edit only). Runtime hook / mandatory resolver invocation tracked separately in the 008 Task-dispatch row above. | partially resolved — PRD 012 R6–R8 shipped (PR #79); platforms dual-map + interactive picker remain deferred |
| GAP-015 | 2026-06-25 | /sw-cleanup user review | 004 | **`/sw-cleanup` confirm step is a manual bash hand-off, not an agent-driven prompt:** after the dry-run report, the command tells the user to run `bash scripts/cleanup.sh --confirm --yes` themselves. Preferred UX: the agent asks the user to confirm (yes/proceed), then runs the apply step on their behalf — keeping the human gate but removing the manual terminal command. **Follow-up:** update `core/commands/sw-cleanup.md` so the confirm path is an agent prompt → agent runs `bash scripts/cleanup.sh --confirm --yes` (or `SW_CLEANUP_CONFIRM=1`) after explicit ack; preserve all fail-closed protections (unmerged/default/current branch, in-flight deliver run-state, no `rm -rf`); confirm only deletes the `wouldRemove` set the user reviewed; sync dist emit. | resolved — PRD 011 R8–R10 (PR #76) |
| GAP-016 | 2026-06-25 | /sw-doc PRD 010 run + user review | — | **Frozen PRDs are not auto-committed to `main`, creating a data-loss window:** `/sw-freeze` stamps `frozen: true` in frontmatter but does not commit the artifact to any branch. The files exist only as untracked/modified working-tree entries until `spec-seed` eventually runs at `deliver-loop` entry — which may be hours or sessions later. An accidental `git checkout -f`, `git clean -fd`, or stash-drop destroys PRD Q&A that cannot be trivially regenerated. **Root cause:** `spec-seed` seeds onto the feature branch, not `main`; no step currently commits docs to `main`. **Proposed fix:** extend `/sw-freeze` (or add a post-freeze hook) to automatically commit the frozen artifact to the current default branch (`main`) immediately after stamping — docs-only commit, no implementation content. Constraints: must not commit brainstorm files (`docs/brainstorms/`) per `spec-seed` exclusion logic; must be idempotent (no-op if already committed); must not block or modify the freeze verdict. Frozen artifacts on `main` are safe: PRD folders are isolated per `<n>-<slug>`, feature branches inherit them without conflict, and `spec-seed` already handles the already-seeded case as a no-op. | planned — PRD 013 R1–R5 (frozen). **Design (PRD 013 DL-1):** commit onto `<type>/<slug>`, NOT `main` |
| GAP-017 | 2026-06-25 | /sw-doc PRD 010 run + user review | — | **`deliver-loop` supports only one concurrent PRD; parallel product-area work is blocked:** `wave_deliver_loop.py` uses a single repo-wide state file (`.cursor/sw-deliver-state.json`) and a single repo-wide orchestrator lock (`.cursor/sw-deliver.lock`). `assert_run_identity` rejects any `deliver-loop` call whose `--task-list` differs from the in-progress `source_task_list` (exit 20, `stale-state` halt); `lock acquire --nonblock` (O_CREAT\|O_EXCL) fails if the lock file exists regardless of target branch. This forces strict sequential delivery: one PRD must fully merge before the next can start. Teams working on independent product areas (e.g. PRD 003 review adapter + PRD 010 memory provider) cannot implement them concurrently, even though the feature branches are fully orthogonal. **Proposed fix:** scope state and lock per target branch — e.g. `.cursor/sw-deliver-state.<slug>.json` and `.cursor/sw-deliver-<slug>.lock` — so independent orchestrators can run in parallel for disjoint feature branches. Requires auditing all state/lock readers to accept the scoped path (via `--task-list` resolution), updating `assert_run_identity` to scope identity checks, and adding a concurrent-run index so `/sw-cleanup` and `/sw-status` can enumerate all live runs. Living-doc contention (`INDEX.md`, `CHANGELOG.md`) remains a serialization concern within each wave and must still be handled by the contention model, not the lock. | planned — PRD 013 R6–R12 (frozen) |
| GAP-018 | 2026-06-26 | User review — memory vs repo duplication + `/sw-compound-ship` | — | **Provider-conditional source of truth (R32 / KTD3 revisit):** When `memory.provider` is `recallium` (or another external provider), operators expect memory to be the sole authority for decisions; today frozen `docs/decisions/<n>-<slug>.md` records stay authoritative in git while Recallium `decision` memories are pointers/distillations only (`skills/memory/SKILL.md`, `sw-memory-audit`). `.gitignore` keeps decision records local-only (only `docs/decisions/INDEX.md` tracked), so untracked `docs/decisions/*.md` in GitHub Desktop plus overlapping Recallium DECISION entries reads as duplication. Early Recallium design memories (#2059/#2068) also conflict with current R32 policy. **Proposed policy:** external memory provider enabled → memory is SoT for decisions (and possibly other doc classes); `in-repo` or no provider → repo is SoT. **Must resolve:** freeze/CI contract, worktree propagation, PR review surface, `docs/decisions/` gitignore vs tracked deliverables, supersede reconciliation (`SUPERSEDED.log`), pointer-only `/sw-compound` rules, and `/sw-memory-audit` conflict resolution. **Should close:** yes — current dual-layer model confuses operators. **Follow-up:** `/sw-brainstorm` → new PRD (broader than PRD 010 MemPalace provider swap); reconcile stale memories via `/sw-memory-audit`. Recallium memories #2164 (learning), #2165 (design). | resolved — PRD 015 R1–R12 (PR #98, PR #116) |
| GAP-019 | 2026-06-26 | User review — compound command naming | 004 | **`/sw-compound` naming does not match operator mental model:** the compounding step is a retrospective (retro → learnings → memory/status), but the command name reads as a generic "compound" with no tie to post-delivery reflection. **Proposed fix:** rename to `/sw-retrospective` (or similar — e.g. `/sw-retro` if keeping the atomic `/sw-retro` report step distinct). Update `core/commands/`, dist emit, `workflow.config.json` routing, conductor/deliver handoffs, and any fixtures referencing `sw-compound`. Preserve backward-compat alias or one-release deprecation note if external docs reference the old name. | resolved — PRD 014 R1–R5 (PR #82) |
| GAP-020 | 2026-06-26 | User review — compound command surface | 004 | **`/sw-compound` + `/sw-compound-ship` overlap looks like an oversight:** two similarly named commands where the orchestrator (`compound-ship`) chains `retro → compound → memory-sync → status` and the atomic (`compound`) performs one bounded write step. Operators cannot tell which to run or when. **Proposed fix:** collapse to a single user-facing command (likely the renamed `/sw-retrospective`) with internal phase dispatch — `--pre-merge` / `--post-merge` flags for deliver in-loop vs standalone; atomic sub-steps (`retro`, `memory-sync`, `status`) remain script/skills-backed but not separate top-level commands unless a genuine escape hatch is needed. **Must resolve:** R17–R20 pre-merge `completed-pending-merge` semantics, R19 fail-closed memory writes, rule-class human gates (R21/R42), and deliver conductor `terminal-ship` handoff without duplicating procedures. | resolved — PRD 014 R1–R4, R9 (PR #82) |
| GAP-021 | 2026-06-26 | User review — deliver terminal sequencing | 004 | **Retrospective/compounding not auto-run before terminal PR in `/sw-deliver`:** today `compound-ship --pre-merge` is a later deliver phase that may halt for human ack; the user expects retrospective to run automatically at the end of deliver, **before** the feature-branch PR to `main` is opened. **Proposed fix:** wire deliver-loop terminal path so all phases `green-merged` → auto-invoke retrospective chain (no re-prompt) → then `terminal-pr` / PR create. Ordering: implementation complete on `<type>/<slug>` → retrospective artifacts committed on feature branch → open PR → CI watch. Retrospective must not wait for merge to `main`. | partially resolved — PRD 014 `/sw-retrospective` + conductor handoff shipped (PR #82); PRD 013 A1 R20–R21 deliver-loop auto-invoke still open |
| GAP-022 | 2026-06-26 | User review — deliver terminal PR automation | 004 | **Terminal PR + CI watch + stabilize should run autonomously; merge to `main` must stay human-gated:** at deliver end the feature branch needs an open PR to `main`, then `/sw-watch-ci` (bounded wait) and `/sw-stabilize` on `red`/`blocked` — all without user intervention. **Must not:** auto-merge, force-push `main`, or dismiss failing checks. **Proposed fix:** extend deliver-loop `terminal-ship` (or successor) to: create/update PR via existing `phase-pr` path → push head → `check-gate.sh` loop with stabilize remediation budget (`deliver.remediation.maxAttempts`) → halt only at legitimate gates (exhausted remediation, destructive git, explicit `supervised` checkpoint). Conductor in-turn self-continuation (R6/R13) owns the watch/stabilize cycle until `green` or halt. | planned — PRD 013 A1 R22–R24 (frozen) |
| GAP-023 | 2026-06-26 | User review — PR test plan vs CI | 004 | **Manual PR test plans are not enforced by CI — `/sw-watch-ci` cannot catch doc/workflow regressions:** FEAT→`main` PRs routinely list a test plan (e.g. `run-ux-polish-fixtures.sh`, `run-doc-fixtures.sh`, `run-cleanup-fixtures.sh`, `docs-link-check.sh`) in the description, but those commands run only if an agent executes them manually. Failures surface after merge or not at all. **Proposed fix:** promote recurring test-plan items to CI jobs (required or all-checks policy per `checks-gate`) so every PR head runs the same fixtures; PR template becomes a link to CI job names, not a manual checklist. **Follow-up:** audit which fixtures are PR-blocking vs advisory; align `verify.test` / workflow YAML with the standard FEAT test-plan set; ensure stabilize can consume job logs via existing `check-gate.sh` path. | resolved — PRD 016 R1–R7 (`pr-test-plan.manifest.json`, `.github/workflows/pr-test-plan-ci.yml`, `run-pr-test-plan-fixtures.sh`), PR #80 |
| GAP-024 | 2026-06-26 | User review — compound-ship autonomy | 004 | **`/sw-compound-ship` still has human approval gates that block full autonomy:** pre-merge mode commits learnings and records `completed-pending-merge`, but compound approval gates and post-merge reconcile (`--require-merge`) expect a human merge before `INDEX` → `complete`. **Question:** can deliver assume implementation is complete once the branch exists and terminal PR is open (or green), and run the full compound chain without "did you merge?" prompts? **Proposed policy options:** (A) **pre-merge autonomous** — run retro/compound/memory-sync/status + file commits on feature branch when terminal PR is `green`, treat merge as external; post-merge `reconcile` triggered by merge detection (R53/R56) without re-prompt; (B) **optimistic post-merge** — after PR merge detection only (risky if human declines merge). **Must preserve:** R19 fail-closed memory, R21 rule-class human gate, no false `complete` on declined merge (R53). **Follow-up:** PRD amendment or new PRD for autonomy knobs (`compound.autonomy`, `doc.afterTasks`-style). | resolved — PRD 014 R10–R11 (PR #82); PRD 013 A1 R24 consumption pending |
| GAP-025 | 2026-06-26 | User review — cleanup autonomy | 004 | **`/sw-cleanup` still requires explicit human confirm before apply:** PRD 011 R8–R10 improved UX (agent prompts then runs `cleanup.sh --confirm --yes`), but fully autonomous deliver cannot prune merged branches/worktrees without a yes/proceed. **Question:** can cleanup run without interaction when merge is detected (R56)? **Proposed fix:** add an autonomous path — e.g. deliver post-merge-detection hook or `cleanup.autonomy: auto` — that applies only the dry-run `wouldRemove` set when: branch merge status is deterministic (not `indeterminate`), no in-flight deliver lock/journal, not current/default branch. Log full report; never delete unmerged or protected items. **Alternative:** keep human gate but auto-suggest + single ack in supervised mode only. Relates to resolved PRD 011 agent-driven confirm; this gap is **zero-interaction** cleanup after known-good merge. | planned — PRD 013 A1 R25–R26 (frozen) |
| GAP-026 | 2026-06-26 | /sw-deliver PRD 012 run + user review | 011 | **Deliver mid-run resume/handoff still emits raw `bash` instead of `/sw-*` commands:** during PRD 012 phase-mode deliver, the agent halted with `Resume: bash scripts/wave.sh deliver-loop --task-list docs/prds/012-…/tasks-….md` rather than an invokable `/sw-deliver run …` the user can paste into chat. PRD 011 R1–R4 fixed the **post-freeze** path in `sw-doc.md` (`/sw-deliver run` primary; bash only as documented underlying driver), but **in-loop deliver resume** is still bash-first. **Audit — user/agent-facing handoffs that should be `/sw-*`:** (1) `scripts/wave_failure.py` `resume_deliver_command()` and `wave_deliver_loop.py` blocker fallback → `/sw-deliver run <source_task_list>` (omit path only when durable state already holds `source_task_list`); (2) `core/skills/conductor/SKILL.md` consolidated halt examples + agent output contract → same; (3) `core/commands/sw-deliver.md` procedure block still shows bash as the illustrated entry/resume — demote to internal driver note, surface `/sw-deliver run` / resume-via-state; (4) `core/skills/deliver/SKILL.md` orchestrator restart line → `/sw-deliver run`; (5) scan other commands for primary next-step bash (`sw-compound-ship`, `sw-ship`, `sw-status`, `sw-debug`, `sw-cleanup` already mostly `/sw-*`; `sw-ship` uses `--from`, good). **Keep bash for:** conductor in-turn mechanical re-invocation, agent-internal steps, documented "underlying driver" footnotes — not user copy-paste resume text. **Follow-up:** implement resumeCommand mapping in wave scripts; update conductor/deliver command output contracts; add deliver fixture asserting `resumeCommand` matches `^/sw-deliver run ` when `source_task_list` present; dist emit. | planned — PRD 017 A1 R29 (operator-facing deliver resume/handoff emits `/sw-deliver run`; `wave_failure.py resume_deliver_command` + conductor/deliver/`sw-deliver.md` docs; bash retained only for mechanical re-invocation/footnotes), amendment frozen 2026-06-26 (parent not-started) |
| GAP-027 | 2026-06-26 | /sw-deliver PRD 016 run + user review | 009 | **Conductor paused after post-remediation green-merged phase ("status-pause" escape vector):** after phase 1 reached `green-merged` via a stabilize fix commit (post-merge verify failure on `cursor-golden-vs-dist`), the agent ended its turn with a "Note: [remediation event] — Want me to continue with phase 2?" prompt instead of immediately re-invoking `deliver-loop`. R11 prohibits per-phase completion halts but (1) does not explicitly cover phases that required remediation before `green-merged`, and (2) does not block the "status note + scope confirmation" framing pattern the agent used as a proxy halt. The agent self-described the pause as "an agent choice about scope, not a deliver requirement" — meaning R11 was not categorical enough to close the escape hatch. **Proposed fix:** harden conductor R11 with an explicit post-remediation bullet (a phase whose final `status.json` is `merge-ready-green` is complete regardless of the path it took) and a new R11a "no-status-pause framing" prohibition (while `verdict: running`, emitting a note + scope-confirmation is forbidden; remediation context goes to `run.log`); add positive obligation — re-invoke `deliver-loop` before any user-visible text after `dispatch-ship`. Mirror in `/sw-deliver` conductor in-turn loop section. **Should close:** yes — autonomous mode is meaningless if a single remediation event can trigger an unguarded scope pause. | planned — PRD 017 R14–R15 (categorical no-status-pause framing + post-remediation `merge-ready-green` completeness), task 2.4, fixtures `conductor-no-status-pause` + `conductor-post-remediation-complete` (frozen, not-started); status corrected 2026-06-26 GAP triage |
| GAP-028 | 2026-06-26 | /sw-deliver PRD 016 run + user review | 007 | **`compoundShip` / `record-premerge` writes orchestrator worktree state but repo-root `.cursor/sw-deliver-state.json` stays stale:** recurring agent notice: "The ROOT state file lacks compoundShip. record-premerge wrote to the orchestrator's `.cursor/sw-deliver-state.json` instead." **Not a missing `workflow.config.json` knob** — durable deliver state has two physical copies after orchestrator provision (repo root + `.sw-worktrees/<slug>-orchestrator/.cursor/`). `wave_deliver_loop.py` persists via cwd-relative `save_state(root, …)`; `/sw-compound-ship --pre-merge` → `wave_compound.py record-premerge` uses the same cwd-relative path, so when the agent runs compounding inside the orchestrator worktree, `compoundShip` + `completion.completed-pending-merge` land only there. **Read path is partially fixed:** `cleanup_lib.resolve_deliver_state` prefers the orchestrator copy when newer/terminal (PR #75), but **write path is not unified** — `deliver-loop` / agents reading repo-root state miss `compoundShip`, can re-offer `compound-ship`, or mis-report terminal progress. **Symptoms:** root state shows all phases `green-merged` but no `compoundShip`; orchestrator copy has `compoundShip.premergeDone`; stale root `verdict: running` may linger after terminal PR. **Proposed fix:** (1) centralize canonical state resolution in `wave_state.py` / shared helper (read + write); (2) `record-premerge` (and any compound write) mirrors to repo-root state when `orchestratorWorktree.path` is set, or always writes canonical path from `resolve_deliver_state`; (3) document single source of truth in `.sw/layout.md`; (4) fixture: after pre-merge record from orchestrator cwd, repo-root state must contain `compoundShip.premergeDone`; (5) optional: teardown sync or delete stale root copy on orchestrator provision. Relates to PRD 013 scoped-state gap (row 24) but distinct — this is same-run dual-copy drift, not concurrent PRD locking. | planned — PRD 013 A2 R28 (canonical deliver-state write path with read/write parity across the orchestrator-worktree-vs-repo-root axis; `record-premerge`/compound writes resolve via the shared resolver; `.sw/layout.md` SoT + fixtures), amendment frozen 2026-06-26 (parent not-started) |
| GAP-029 | 2026-06-26 | /sw-deliver PRD 014 run + user review | 014 | **Conductor paused mid-phase during `dispatch-ship` (incomplete ship work + turn boundary):** PRD 014 phase 2 (`deprecated-aliases-rename-propagation-m`) was in-flight with `verdict: running` and `nextAction: dispatch-ship`; phase worktree provisioned, deprecation shims drafted locally but not committed, rename propagation (sw-naming.mdc, deliver/conductor skills, fixtures, dist) still open. The agent ended its turn with a status report and "Want me to continue with phase 2 ship?" instead of completing `/sw-ship --phase-mode` (implement → verify → commit → `merge-ready-green` status → merge) and immediately re-invoking `deliver-loop` per conductor R6/R13. **Not a legitimate halt** — no `halt: true`, no blocker verdict, no configured `supervised` checkpoint; pause was incomplete agent work plus turn boundary. **Secondary hygiene (not current blocker):** parallel ceiling hit earlier (4/4) because stale PRD 016 phase worktrees (`pr-test-plan-ci-enforcement-orchestrator`, `pr-test-plan-ci-enforcement-phase-docs-dist-fixtures-s-m`) consumed slots alongside PRD 014 orchestrator; phase 1 worktree torn down after merge, ceiling no longer blocks the existing phase 2 worktree but would block provisioning another concurrent phase until cleanup (`/sw-cleanup`). **Proposed fix:** (1) extend R11/R11a (see PRD 016 status-pause row) to explicitly forbid mid-phase `dispatch-ship` scope-confirmation prompts while `verdict: running` — conductor must run the full ship chain in the phase worktree before any user-visible pause; (2) positive obligation: after `awaitAgent: true` + `dispatch-ship`, agent completes ship work and re-invokes `deliver-loop` in the same turn (no "resume?" framing); (3) optional deliver hygiene: pre-provision slot check or auto-suggest `/sw-cleanup` when `parallel ceiling reached` is logged; (4) fixture: deliver-loop returns `dispatch-ship` → agent must not emit halt prose without `status.json` `merge-ready-green` or `blocked`. **Should close:** yes — same escape vector as PRD 016 status-pause but on the in-phase ship path. Relates to row 34 (post-remediation pause) and row 33 (bash resume handoff). | planned — PRD 017 R14/R15/R16/R28 (no user-visible text after `dispatch-ship` until terminal `status.json`; driver-detected-halt-only), task 2.4, fixture `conductor-reinvoke-after-dispatch-ship` (frozen, not-started); status corrected 2026-06-26 GAP triage |
| GAP-030 | 2026-06-26 | User review — deliver worktree lifecycle | 004 | **Phase worktrees not torn down immediately after `green-merged` — linger until end-of-run `/sw-cleanup`:** operator expectation: once a phase branch is merged into `<type>/<slug>`, its isolated phase worktree is no longer needed and should be removed right away (`git worktree remove` + `prune`, R21). **Today:** `scripts/wave.sh phase-teardown` exists and PRD 007 TR1 documents the deliver-loop cycle as `merge run-next` → bookkeeping → verify-after-merge → forward-merge → **`phase-teardown`**, but `wave_merge.py` `merge-run-next` stops after incremental verify — it never invokes teardown; agents may tear down ad hoc (PRD 014 phase 1) or not at all. Merged phase worktrees accumulate under `.sw-worktrees/` for the entire feat run (and across runs if `/sw-cleanup` is deferred), consuming `worktree.parallelCeiling` slots (default 4) and blocking concurrent phase provision for other PRDs. **Symptoms:** `parallel ceiling reached (4/4)` with orchestrator + in-flight phase + stale merged-phase worktrees from prior PRDs; manual `/sw-cleanup` required mid-run. **Proposed fix:** (1) wire automatic `phase-teardown` into `merge-run-next` immediately after `verify run-after-merge` passes (before advancing dependents); (2) clear `phaseWorktrees[<id>]` from deliver state on teardown; (3) optional: prune merged phase branch when safe (no open dependents, not current branch); (4) keep orchestrator worktree until terminal PR / run completion; (5) conductor/deliver-loop: add `phase-teardown` as a mechanical `awaitAgent: false` step or inline in merge path; (6) fixture: after `green-merged`, phase worktree path must not exist and ceiling slot count decrements. **Must preserve:** durable `status.json` already collected before merge; forward-merge uses orchestrator checkout, not the phase worktree post-teardown. **Should close:** yes — R21 is spec'd but not enforced in the driver. Relates to row 36 (ceiling symptom), row 32 (end-of-run cleanup autonomy). | planned — PRD 017 R17 (safe eager teardown: `green-merged → teardown-pending → teardown-complete` wired into the merge path after verify + dependent forward-merge), task 2.5, fixture `phase-teardown-eager-safe` (frozen, not-started); status corrected 2026-06-26 GAP triage |
| GAP-031 | 2026-06-26 | User review — memory-before-work enforcement | — | **Pre-work memory search not a categorical, enforced obligation (consistency risk):** before performing ANY substantive change, an agent should search memory for relevant **rules, prior decisions, similar changes, and learnings** tied to the surface being touched, so memory is actually leveraged for consistency across the development lifecycle. Today this is **best-effort, not enforced**: `sessionStart` injection is explicitly best-effort (and only fail-closed for guardrails at `beforeSubmitPrompt`, not for reads), and `skills/memory/SKILL.md` says "read before substantive work" as guidance per command rather than a mechanical pre-work gate. Result: an agent (or sub-agent dispatched mid-loop) can implement, debug, or author without ever querying memory, re-deriving decisions that already exist and drifting from established rules/learnings. The memory **location is provider-variable** (`memory.provider`: `in-repo` or `recallium`), so the obligation must be expressed against the provider-agnostic `memory-preflight` adapter (`providers/<memory.provider>.md`) — never a direct provider call (per `sw-guardrails`). **Proposed fix:** (1) define a mandatory pre-work memory-search step (a `memory-preflight search` scoped to the touched files/feature + relevant classes: `rule`, `decision`, `learning`, `code-context`, `design`) at command/skill entry for work-performing commands (`/sw-execute`, `/sw-debug`/`rca-core`, `/sw-prd`/`/sw-brainstorm`, `/sw-amend`, `/sw-review`/`/sw-stabilize`) and at **sub-agent dispatch** (`sw-subagent-dispatch.mdc`) so delegated agents inherit the obligation; (2) surface the hits to the agent before it acts (and require it to reconcile against found rules/decisions); (3) keep it provider-agnostic via the resolved adapter; (4) decide enforcement strength — guidance/rule vs a fail-closed entry gate analogous to `beforeSubmitPrompt` — and whether a "no relevant memory found" result is recorded; (5) fixture asserting work-performing command/sub-agent entry routes a `memory-preflight search` before mutation. **Should close:** yes — best-effort reads undermine the compounding-memory premise. **Follow-up:** `/sw-brainstorm` → PRD (or amend the memory workstream / PRD 015); relates to memory SoT (row 25) and the redaction/guardrail chokepoint, but distinct — this is the *read-before-work* obligation, not the store/SoT contract. | planned — PRD 019 R1–R10 (mandatory scoped `memory-preflight search` at work-performing command entry + sub-agent dispatch inheritance; provider-agnostic; degrade-open probe-gated breadcrumb; `preToolUse` deny reusing PRD 017 R23), frozen 2026-06-26 (not-started) |
| GAP-032 | 2026-06-26 | User review — `copy-to-core.sh` / build-chain asymmetry | 004 | **`copy-to-core.sh` destructive sync vs split source-of-truth (investigate before fixing):** running `bash scripts/copy-to-core.sh` can produce large unstaged `core/` diffs unrelated to the Cursor plugin install (`~/.cursor/plugins/local/shipwright/` is a separate tree — copy-to-core does **not** read from it). Two repo-internal drift modes observed: (1) **`scripts/` ahead of `core/scripts/`** — runtime harness (`wave.sh`) uses repo-root `scripts/`; PR merges (e.g. PR #80) can land harness changes without a matching `copy-to-core` + commit, so re-sync exposes committed mirror lag (`run-core-scripts-parity-fixtures.sh` catches this but only if run). (2) **`core/sw-reference/` superset vs `.sw/` subset** — `.sw/` today has ~4 files; `core/sw-reference/` also holds PR-authored artifacts committed directly to `core/` only (e.g. `pr-test-plan.manifest.json`, `model-tier-hook-feasibility.md`). `copy-to-core` rsyncs `.sw/` → `core/sw-reference/` with `--delete`, removing core-only files except two excluded `*.defaults.json`; workflow config may reference `core/sw-reference/…` paths. **Risk if patched hastily:** shipwright-specific excludes or hand-edits to `core/` that do not generalize to consumer repos using the same emitter chain. **Investigation scope (decide path, then PRD):** (A) canonical SoT map — what must live in `.sw/` vs `scripts/` vs `core/` only; (B) whether `copy-to-core` should be non-destructive, two-way, or fail-closed on orphans; (C) enforce `copy-to-core` + commit in deliver/ship dist phase vs CI parity gate only; (D) relocate core-only sw-reference artifacts into `.sw/` (or document as generated); (E) clarify plugin install vs repo `core/`/`dist/` relationship in docs so agents do not conflate the three trees. **Constraint:** any fix must apply to the **consumer-repo build chain** (`scripts/` → `core/` → `dist/`), not Cursor-plugin-install-only behavior. Relates to resolved row 13 (`core/scripts/` parity) but broader — includes `sw-reference` delete hazard. | open — investigate; no PRD until SoT decision |
| GAP-033 | 2026-06-26 | /sw-cleanup post-PRD 015 investigation | 004 | **`/sw-cleanup` apply order deletes branches before worktrees — predictable failure for integration branch:** `cleanup_lib.py` `enumerate_cleanup()` lists local branches first, then remotes, then worktrees; `apply_report()` processes `would_remove` in that same order and runs `git branch -D` before `git worktree remove`. When the integration branch (`feat/<slug>`) is still checked out in the orchestrator worktree (`.sw-worktrees/<slug>-orchestrator`), branch delete fails (`cannot delete branch '…' used by worktree at …`) even though the worktree is queued for removal in the same pass — exit code 1 and an orphaned branch until manual `git branch -D`. **Symptoms:** recurring after every phase-mode deliver terminal cleanup (PRD 015: `feat/memory-source-of-truth` left behind). **Proposed fix:** (1) reorder apply — worktrees and run-state before branches/remotes; or (2) skip branch delete when a queued worktree still holds the ref; (3) fixture: branch + orchestrator worktree on same ref in one `apply_report` pass must succeed without errors. Relates to row 37 (orchestrator persists until end-of-run), row 32 (cleanup autonomy). | resolved — `apply_report` sorts worktree → run-state → remote → branch (2026-06-27) |
| GAP-034 | 2026-06-26 | /sw-cleanup post-PRD 015 investigation | 004 | **Cleanup merge detection compares phase branches to `main`, not integration branch — stale `feat/*-phase-*` branches stay protected:** `merged_status()` in `cleanup_lib.py` classifies branches via ancestor-of-default, empty `main..branch` log, `git cherry main branch`, then `gh pr list --state merged`. Deliver merges phase branches into `<type>/<slug>` first; only the terminal PR squash-merges integration into `main`. Phase commits are never ancestors of `main`, so `git cherry` typically shows `+` lines → `unmerged` / `cherry-plus` → protected forever unless `gh` reports a merged PR for that exact head (`host-merged`). **Symptoms:** ~14 older `feat/*-phase-*` local branches accumulate across deliver runs; recent PRD 015 phases cleaned via `host-merged` but older PRs without reliable `gh` signal remain. **Proposed fix:** (1) for phase-mode deliver artifacts, compare against integration branch (`feat/<slug>`) or walk deliver state `mergedPhases` / phase PR records when classifying merge status; (2) optional: use deliver journal for deterministic squash into integration; (3) preserve R56 fail-closed for truly indeterminate cases; (4) fixture: phase branch merged to integration via regular merge but not individually on `main` must be `wouldRemove`, not `protected`. Relates to row 37 (phase branches linger), R56 squash-merge policy. | partially resolved — `parent_wave_branch` + `parent-wave-merged` in `cleanup_lib.py` (2026-06-27); open PR on phase head still blocks via `host-open-pr` |
| GAP-035 | 2026-06-27 | /sw-deliver PRD 021 run + PR #129 investigation | 021 | **Phase-mode `/sw-ship` opens orphan PR to `main` — deliver merges locally, terminal wave lands on `main`, PR left open:** PRD 021 phase 6 (`migration-with-parity-shadow-cutover-call-site-map-l`) completed via `/sw-ship --phase-mode` in [deliver thread](acaa9924-8216-48bb-9469-66f2167ab150). Subagent ran `gh pr create` **without `--base feat/capability-manifest-and-selector`**, so GitHub opened **PR #129** (`feat/…-phase-migration-…` → `main`). Deliver then merged the phase into the integration branch (`merge run-next`, CHANGELOG `412e81d`) and terminal **PR #131** squash-merged the full wave to `main` — phase 6 tasks 6.1–6.5 are on `main` and checked in the frozen task list. **PR #129 stayed open** (`host-open-pr`), blocking `/sw-cleanup` until manually closed. **Contrast:** phase 8 ship used `--base feat/capability-manifest-and-selector` (PR #130). **Root cause:** `sw-ship` phase-mode contract (R18/R19) says phase-mode never merges to `main` and `/sw-deliver` owns phase→integration merges, but the ship chain still runs `phase-pr` / `gh pr create` with implicit `main` base when integration branch is not injected. **Proposed fix:** (1) in phase-mode, `phase-pr` MUST target `<type>/<slug>` from deliver state (or skip PR creation entirely — rely on `merge-ready-green` + `merge run-next` only); (2) fail closed if `SW_PHASE_MODE` and base ≠ integration branch; (3) on `green-merged` / terminal wave merge, auto-close open phase-head PRs to `main` as superseded (idempotent); (4) fixture: phase-mode ship with integration branch in env → no PR to `main`; (5) document in `sw-ship.md` phase-mode `phase-pr` base contract. **Should close:** yes — orphan PRs pollute GitHub and block cleanup. | open — PR #129 closed manually 2026-06-27; automation fix not yet implemented |
| GAP-036 | 2026-06-26 | User review — documentation-impact review persona | — | **No review persona ensures documentation artifacts affected by a change are updated:** when a change is made in the repo, the in-repo documentation surface (`README.md`, `docs/guides/*`, command/skill docs under `core/commands/` + `core/skills/`, `AGENTS.md`, `INVARIANTS.md`, config/schema docs, `.sw/layout.md`, rule prose) can silently fall out of date. Today this is only **partially** covered: living-doc currency gates keep `INDEX.md`/`COMPLETION-LOG.md`/`GAP-BACKLOG.md` accurate (PRD 009), `docs-presence`/`docs-currency` hooks and `docs-link-check.sh` check presence/links, and the `comment-accuracy` code-review agent checks in-code comments — but **no persona reviews the broader doc surface, maps a change to the doc artifacts it affects, and recommends concrete documentation updates for inclusion in the spec.** **Proposed fix:** add a new **documentation-impact / doc-currency review persona** (e.g. `sw-docs-currency-reviewer`, lens: "given the change set / proposed spec, which in-repo documentation artifacts are affected, and what updates are required?") to the doc-review persona registry + signal-driven selection (`skills/doc-review/SKILL.md`), with a `models.routing.agents` tier entry (R9-compliant; `build`/`mid`). **Decide:** (1) where it runs — doc-review panel (spec-time, so recommendations land in the PRD/tasks as requirements) and/or code-review/ship (change-time, against the actual diff); (2) its output contract — recommended doc updates that fold into the spec (PRD requirement / task) vs advisory findings only; (3) selection signal (always-on for changes touching documented surfaces, or signal-gated like security/design); (4) relationship to the existing living-doc currency gate (this persona is broader: arbitrary doc artifacts, not just the three living indexes). **Should close:** yes — keeps documentation in lockstep with changes via the same review machinery. **Follow-up:** `/sw-brainstorm` → PRD (or amend the doc-review/code-review workstream); add persona file + registry + routing + selection fixture + output-contract fixture; relates to PRD 018 R18/R33 (per-PRD doc tasks) but distinct — this is a *reusable reviewer*, not a one-off doc task. | planned — PRD 020 R1–R9 (`sw-docs-currency-reviewer` spec-time persona; always-on core + amendment floors; maps spec change → affected doc artifacts; recommendations fold into spec via synthesis; `build` tier; complementary to PRD 009 living-doc gate), frozen 2026-06-26 (not-started) |
| GAP-037 | 2026-06-27 | /sw-deliver PRD 023 run + user review (PRs #140/#141) | 023 | **Dangling phase-mode PRs recur — TWO distinct non-close modes confirmed (escalates row 42):** PRD 023 phase-mode deliver left **PR #140** (Phase 4, `feat/…-phase-benefit-metric-…`) and **PR #141** (Phase 5, `feat/…-phase-plan-surfacing-…`) open after the integration branch `feat/deliver-plan-policy-pilot` was fully assembled and terminal **PR #143** (`feat/…` → `main`, squash-merged SHA `7711574`) landed. Both closed manually 2026-06-27 after verifying their content is byte-identical / ancestor-contained in #143; new GAP item per user request. **Two separate root-cause modes observed in one run — the key new finding:** **(A) implicit-`main`-base (row 42 mode):** PR #141 was opened with **base `main`** instead of the integration branch — exactly the `gh pr create` without `--base <integration>` defect in row 42 #1/#2. **(B) head-not-ancestor after orchestrator rewrite (NEW):** PR #140 was correctly based on `feat/deliver-plan-policy-pilot`, but its content reached the integration branch under **different commit SHAs** because the deliver orchestrator regenerated `dist/` + golden manifest itself (and a forward-merge conflict resolution rewrote the phase branch). The remote phase head (`66b1faa`) therefore never became an **ancestor** of the integration tip, so GitHub could not auto-close it (`host-open-pr`) — even though `git diff <integration> 66b1faa -- core/scripts/wave.sh dist/.../wave.sh` is **empty** (fully integrated). **Contrast:** Phase 6 **PR #142** had the correct base AND its head WAS an ancestor → GitHub auto-marked it **MERGED** with no manual action. This proves the row 42 fix #3 ("auto-close open phase-head PRs as superseded") **must NOT rely on head-is-ancestor detection** — it must close by **branch identity from deliver state** (`mergedPhases` / phase PR records), because orchestrator dist-regen + forward-merge legitimately leave the remote phase head as a non-ancestor while content is fully present (same ancestry trap as resume-reconcile, Recallium memory #2124). **Required automation (supersedes/extends row 42):** (1) phase-mode `phase-pr` MUST target `<type>/<slug>` from deliver state and **fail closed** when `SW_PHASE_MODE` and base ≠ integration branch (covers #141 / mode A); (2) at `green-merged` and at terminal-wave assembly, deliver MUST enumerate each phase's recorded PR (from deliver state / `gh pr list --head <phase-branch>`) and **close it as superseded by the integration PR** — keyed on branch identity, not commit ancestry (covers #140 / mode B); (3) idempotent + safe (only close PRs whose phase is recorded `green-merged` for THIS run); (4) fixtures: (a) phase-mode ship with integration branch in env → no PR to `main`; (b) after terminal assembly, no open PR exists whose head is a recorded merged phase branch, regardless of ancestry. **Should close:** yes — third+ occurrence (rows 42, this); orphan PRs pollute GitHub and block `/sw-cleanup` (`host-open-pr`, see rows 41/42). Recurring manual-close workaround already captured in Recallium learnings #2181/#2162. **Follow-up:** implement on the PRD 021/024 orchestrator-adoption workstream or a dedicated amendment; cross-links row 42 (PR #129), row 41 (cleanup `host-open-pr` block), row 37 (phase lifecycle). See deliver thread [PRD 023 deliver dangling PRs](afec9c0c-7742-4dbb-8b58-30fa361201f7). | open — recurred on PRD 023 (#140/#141 closed manually 2026-06-27); reinforces + extends row 42 with new head-not-ancestor mode; branch-identity auto-close automation NOT yet implemented |
| GAP-038 | 2026-06-27 | /sw-doc git-host-portability brainstorm + user review (PRD 022 amendment + PRD 013 brainstorm incidents) | — (PRD 013 lineage) | **No spec-mutation safety when a PRD/task list is being implemented in another session/branch:** authoring commands (`/sw-amend`, `/sw-tasks`, `/sw-prd`) can propose or commit changes to a frozen PRD or its task list while that PRD is actively in-flight in a separate deliver run (different worktree, clone, or `main` ideation), with no mechanism to detect the in-flight state or hand a new task off to the running implementation. **Two real incidents:** (a) a PRD 025 amendment authored on `main` proposed a change to PRD 022 while 022 was under active development on another branch; (b) while authoring `docs/brainstorms/2026-06-27-git-host-portability-and-workflow-standardization-requirements.md`, the agent wanted to change in-flight PRD 013. **Root cause (investigated 2026-06-27):** the in-flight signal EXISTS but is (1) **gitignored / local-only** — `.cursor/sw-deliver-state.*.json`, `.cursor/sw-deliver-runs/index.json`, and `.cursor/sw-deliver.lock` (`.gitignore` lines 5–9) are per-checkout and invisible to other sessions/clones; and (2) **never consulted by authoring commands** — `/sw-amend` / `/sw-tasks` / `/sw-prd` perform only a memory pre-work search, with no implementation-state guard (`/sw-amend`'s sole guard is "parent file is never written"). `/sw-status` can surface live runs (`reconcile-status.sh deliver-runs`, `derive --json` `livePhaseStatus`) but nothing gates authoring on it. **Proposed direction (dedicated PRD; PRD 013 deliver-concurrency / freeze-safety lineage):** (1) a **durable, committed** implementation-state signal (e.g. `INDEX.md` status `in-progress`/`in-flight` carrying run id + branch, or a committed registry/lock) so any session — including other clones — can detect an active implementation; (2) an **authoring-time guard** in `/sw-amend` / `/sw-tasks` / `/sw-prd` that reads the signal and, when the target PRD is in-flight, **refuses or explicitly routes** the change instead of silently mutating; (3) a **change-handoff path** for "new task / mid-flight scope change to an in-flight PRD" (queue as an amendment the running conductor picks up at a safe checkpoint, or block until the run terminalizes); (4) define interaction with worktree/clone topology and the existing scoped run-index (PRD 013 R10). **Complementary but NOT sufficient:** the git-host-portability brainstorm's docs-on-a-branch policy (R28–R32) removes one trigger (ideating on `main` beside in-flight worktrees) but provides neither in-flight awareness nor the authoring guard. **Should close:** yes — concurrent authoring vs in-flight implementation can silently desync spec and code. **Follow-up:** `/sw-brainstorm` → dedicated PRD; cross-links `docs/brainstorms/2026-06-27-git-host-portability-and-workflow-standardization-requirements.md` (R28–R32) and PRD 013. | open — investigate; dedicated PRD warranted (PRD 013 lineage), captured 2026-06-27 |
| GAP-039 | 2026-06-27 | /sw-doc PRD 026 run — doc-review dispatch | 012/017 lineage | **Single-record dispatch-preflight precludes the documented *parallel* persona panel:** `core/hooks/before_task_dispatch.py` validates bound reviewer / native-panel Task spawns against a **single** overwrite-on-write record (`.cursor/hooks/state/task-dispatch-preflight.json`) that is **consumed** (`consumedAt` set) on the first matching spawn and is keyed to **one exact `agent` id**. `skills/doc-review/SKILL.md` (R28/R31) and the conductor describe the persona panel as **parallel** sub-agents, but because each spawn consumes the lone record and the record matches only one agent id, batching N reviewer Task calls in one turn is impossible — only the first passes; the rest fail `stale-preflight-nonce` / `preflight-agent-mismatch`. Bound dispatch is therefore **strictly sequential** (preflight → one spawn → preflight → …), and `worktree.parallelCeiling` (4) is moot for reviewers. **Symptom:** PRD 026 doc-review could not run the 8-persona panel in parallel; it ran inline at deep tier instead. **Proposed fix:** (1) make the preflight a keyed list/map (per dispatch-id or per agent) with independent consume + TTL so a batch of parallel reviewer spawns each match their own record; or (2) explicitly document the panel as sequential and update doc-review/conductor prose + ceiling expectations; (3) fixture: N bound-reviewer preflights recorded → N parallel Task spawns all pass. **Should close:** yes — the parallel-panel design is currently unrealizable through the enforced hook. | planned — PRD 024 A2 R38 (keyed parallel dispatch-preflight: N concurrently-valid records keyed by `dispatchId`, consume-by-dispatch-id in `before_task_dispatch.py`; closes the parallel persona-panel block), amendment frozen 2026-06-27 (parent not-started) |
| GAP-040 | 2026-06-27 | /sw-doc PRD 026 run — dispatch binding | 012 lineage | **`resolve-model-tier.sh` returns different tiers for `--command` vs `--agent` on the same surface — enforced binding under-powers delegated authors:** `resolve-model-tier.sh --command sw-prd` → `deep` / `claude-opus-4-8-thinking-high` (`routing.commands`), but `--agent sw-prd` → `build` / `composer-2.5` (`routing.agents` default). `scripts/dispatch-check.sh` and `core/hooks/before_task_dispatch.py` both resolve the **agent** form, so a delegated `/sw-prd` Task — and every `sw-*-reviewer` persona — binds to build-tier `composer-2.5` even though the command routing intends `deep`. **Symptom:** PRD 026 PRD-authoring delegation would have run on `composer-2.5` (build), weaker than the command's `deep` tier; authored inline at deep instead. Reviewers likewise resolve to build. **Proposed fix:** (1) reconcile agent-vs-command tier resolution so a command-backed delegated atomic inherits its command tier (or add explicit `routing.agents` entries for `sw-prd`/`sw-tasks`/etc.); (2) make `dispatch-check` resolve by command when the dispatch carries `--command`; (3) fixture asserting `--command X` and `--agent X` agree, or document an intentional divergence. **Should close:** yes — tier policy is undermined if the enforced binding silently down-tiers delegated authors. | planned — PRD 024 A2 R39 (command-authoritative tier resolution + explicit-agent-override precedence single-sourced in `resolve-model-tier.sh`; closes the `--command` vs `--agent` down-tier), amendment frozen 2026-06-27 (parent not-started) |
| GAP-041 | 2026-06-27 | /sw-deliver PRD 026 run — terminal retrospective halt | 007/013/017 lineage | **Terminal `retrospective` pause after all waves — `all_phases_merged` vs `all_phases_green` status split + agent turn boundary:** PRD 026 reached `nextAction: retrospective` / `verdict: running` with all five phases merged to integration (`feat/git-host-portability-and-workflow-standardization` @ `f769845`), but `/sw-deliver run` stalled with no terminal PR. **Root cause A (mechanical — blocks even in-turn continuation):** `wave_deliver_loop.all_phases_merged()` treats `teardown-complete` as merged (`MERGED_PHASE_STATUSES`), so after eager `phase-teardown-run` (PRD 017 R17) the driver routes to `retrospective`. **`wave_terminal.all_phases_green()` and `wave_compound.all_phases_green()` require strict `status == "green-merged"` for every phase** — phase 3 was left `teardown-complete` (worktree absent fast-path in `wave_lifecycle.py`) while phases 1–2/4–5 stayed `green-merged`. `bash scripts/wave.sh terminal retro run` therefore fails closed (`retrospective requires all phases green-merged`, exit 20) even though deliver-loop already advanced. **Root cause B (agent escape vector):** `deliver-loop` returns `awaitAgent: true` for `next.action: retrospective` (`AGENT_ACTIONS`); conductor R6/R13 requires `/sw-retrospective --pre-merge` + immediate re-invoke in the **same turn**. Agent instead emitted wave-complete status prose (rows 34/36 pattern at the **terminal** transition). **Contributing factors in same run:** `tasks-currency-divergence` when `source_task_list` only on integration worktree; `merge-queue:conflict` on parallel-wave out-of-order merges; post-merge `verify:failed` on integration (fixture harness / worktree ceiling — merge retained); `deliver.terminal.autonomy: supervised` adds supervised checkpoint on `terminal retro run` (compound-ship autonomy row); stale `blockers.json` may still show prior `conductor:no-progress`. **Proposed fix:** (1) unify terminal completeness — `all_phases_green()` accepts `MERGED_PHASE_STATUSES` or teardown preserves `green-merged` + adds `teardownAt`; (2) conductor R11a: `retrospective`/`terminal-ship` `awaitAgent` non-optional; (3) resolve `source_task_list` via orchestrator path for `tasks_currency_ok`; (4) fixture: after teardown on all phases, `terminal retro run` passes. Cross-links rows 28, 31, 34, 36, 37, 48. | open — PRD 026 terminal halt; captured 2026-06-27 |
| GAP-042 | 2026-06-27 | /sw-deliver PRD 026 run — phase 1 status collect | 007 lineage (regression) | **Phase `status.json` path edge case recurs — background dispatch + relative `SW_RUN_DIR` bypasses R38 worktree lookup:** row 10 marked **resolved** (PRD 007 R38: `wave_merge.status_file_for` checks phase worktree `.cursor/sw-deliver-runs/<slug>/status.json` before repo root), but PRD 026 phase-mode deliver still stalled on `await-in-flight` → `conductor:no-progress` after Phase 1 `/sw-ship --phase-mode` completed. **Observed mechanism:** (1) background Task ship runs inside `.sw-worktrees/<slug>-phase-…` with `SW_RUN_DIR=.cursor/sw-deliver-runs/<phase-slug>` **relative to the worktree cwd**; `ship-phase-status.sh` writes `status.json` there; (2) `deliver-loop` / `compute_next_action` reads state from repo root and calls `status_file_for(root, slug, …)` — which only finds the worktree copy when `phaseWorktrees[<id>].path` is present in **that** state snapshot; (3) when lookup misses (stale/missing `phaseWorktrees` on repo-root state — see row 35 dual-copy drift — or slug/state skew), the driver sees no `merge-ready-green`, and phases with `backgroundDispatchedAt` fall through to `await-in-flight` ("awaiting terminal status.json from background Tasks") indefinitely; (4) manual copy to repo-root `.cursor/sw-deliver-runs/<slug>/status.json` or patching state unblocks merge. **Symptom:** ship finished, PR green, but deliver halts with no legitimate human gate. **Proposed fix:** (1) `ship-phase-status.sh` (or `phase-dispatch-env`) MUST write **repo-root-canonical** `status.json` when `SW_REPO_ROOT` / deliver state provides integration root — mirror on write, not discover-on-read only; (2) `status_file_for` fallback: glob `.sw-worktrees/*` for `sw-deliver-runs/<slug>/status.json` when state lookup fails; (3) `await-in-flight` for `backgroundDispatchedAt` phases should proactively run `status collect` (or re-resolve worktree from deliver journal) before sleeping; (4) fixture: background phase ship in worktree → `deliver-loop` next tick reaches `merge-enqueue` without manual copy; (5) reconcile with row 35 — canonical state must carry `phaseWorktrees` on the path `deliver-loop` reads. **Should close:** yes — R38 read-side resolution is insufficient when write-side cwd is the phase worktree and state is split across root vs orchestrator copies. Cross-links row 10 (claimed resolved), row 35 (dual state copy), Recallium #2194. | open — regression on PRD 026 Phase 1 deliver; captured 2026-06-27 |
| GAP-043 | 2026-06-27 | GAP-BACKLOG audit + user review | 009 | **Backlog status feedback loop not mechanical — `planned` rows stay stale after PRD ship:** R49 `gap-resolve` and `docs-currency-gate.sh` only flip/check rows whose Status is exactly `open` with a matching absorbed-by PRD. Rows triaged to `planned — PRD NNN` (2026-06-26) were invisible when PRDs 012/014/015 reached `complete` — confirmed on GAP-009, GAP-014, GAP-018–GAP-020, GAP-024 (manual audit 2026-06-27). **Proposed fix:** (1) single-source status enum in `living-status/SKILL.md` (`open` \\| `planned` \\| `partially resolved` \\| `resolved`); (2) optional `Absorbed-by` column instead of embedding PRD refs in Status prose; (3) extend `gap-resolve` + `docs-currency-gate` to treat `planned — PRD <n>` like `open` when absorbing PRD completes; (4) fixture: PRD ship flips matching `planned` rows; (5) document triage convention — use `open` + Absorbed-by when row is intended for mechanical resolve. **Should close:** yes — living-doc reconcile is meaningless if triage vocabulary bypasses it. | open — investigate; `/sw-brainstorm` → PRD or amend PRD 009 A1 R49 |
| GAP-044 | 2026-06-27 | GAP-BACKLOG audit + user review | 009 | **Stable gap IDs + index for cross-reference (manual contract):** backlog rows lacked stable identifiers; cross-links used fragile "row N" prose and drifted on append. **Done (this edit):** `ID` column (`GAP-001`…), index section above table. **Follow-up to keep durable:** (1) append protocol — next ID is max+1, never reuse; (2) cross-links use `GAP-NNN` not row numbers; (3) optional future: `gap-backlog.sh list --json` / CI guard that index matches table (out of scope for manual minimal fix); (4) update `living-status/SKILL.md` + `.sw/layout.md` to document ID column. **Should close:** when append protocol is documented and cross-link drift eliminated in scripts/skills that reference gaps. | partially resolved — ID column + index added 2026-06-27; append protocol + skill docs still open |
| GAP-045 | 2026-06-27 | /sw-feedback — user review (doc-authoring format divergence) | — (doc-authoring/spec-rigor lineage) | **Model-dependent doc formatting breaks `/sw-prd` / `/sw-amend` / `/sw-tasks` automation — strict regex parsers vs free-form generation, and the parsers disagree with each other:** the doc-authoring chain emits markdown from prose skill instructions (`skills/prd`, `skills/tasks`, `skills/spec-rigor`) but is validated by regex-strict gates that accept only one canonical shape, so freeze/union/traceability pass for some models and fail for others on semantically identical content. **No machine-readable template the model fills and no pre-freeze structural normalizer** exist today (spec-rigor is explicitly "No auto-fix" — it rejects, never canonicalizes). **Verified divergence classes:** (1) **R-ID/D-ID bullets** — `spec-rigor-check.sh` requires exactly `^- \*\*(R\d+)\*\*` (e.g. `- **R12** …`); breaks on `- **R12.** …`, `- **R12:** …`, `* **R12** …`, indented/numbered bullets, `R-12`, or code-span IDs. (2) **Parser disagreement** — `spec-union.sh` accepts THREE forms (`- **R12**`, bare `**R12**`, and `## R12 (…)` headings) that `spec-rigor-check.sh` (single bullet form) rejects, so a PRD can pass union+traceability but fail the checklist gate (or vice versa); confirmed by Recallium learning #2193 where Decision Log authors deliberately write `- **D1.**` to DODGE `spec-union` D-ID extraction — i.e. the canonical real-D-ID shape and the "don't extract me" shape collide. (3) **Section headings** — exact `^##\s+<Name>\s*$` (Overview/Goals/Non-Goals/Requirements/Testing Strategy for PRD; Context/Decision/Rationale/Alternatives/Consequences for decision); breaks on parentheticals/numbering/extra words (`## Requirements (Functional)`, `## Non Goals`, `## 4. Requirements`) → "missing section" fail. (4) **Traceability rows** — each cell must match `^R\d+$`; combined `R1, R2` or ranges `R1–R3`/em-dash silently drop coverage → false "uncovered R-ID" freeze block. (5) **Phase headings** — tasks require `^###\s+(\d+)\.\s+`; breaks on `### Phase 1: …`, `### 1 — …`, wrong heading level → phases undetected → `## Phase Dependencies` "missing row for phase" cascade. (6) **Silent amendment-directive loss** — `parse_frontmatter_list` reads `supersedes:`/`retracts:` only as inline `[a, b]`; a YAML block list (`supersedes:` newline `  - R1`) yields empty and the directive is **dropped with no error**, corrupting the spec union. **Proposed solutions (decide in brainstorm→PRD; do NOT patch this turn — Recallium learning #2172):** (a) a single shared tolerant tokenizer used by `spec-union`, `spec-rigor`, `traceability-check`, and `wave_deliver` phase parsing so they cannot disagree — broaden accepted bullet/heading/range forms OR fail closed early with a precise, line-anchored format error; (b) a deterministic pre-freeze format normalizer (`scripts/doc-format-normalize.sh --check\|--write`) that canonicalizes R/D-ID bullets, section headings, traceability cells (expand lists/ranges), and phase headings before the strict gates (relaxes "no auto-fix" for structural shape only, never content); (c) machine-readable slot-filling templates emitted by `/sw-prd`/`/sw-tasks`/`/sw-amend` so models fill canonical structure instead of free-forming it; (d) make `supersedes`/`retracts` parsing accept YAML block lists OR fail closed when a non-empty key yields zero parsed IDs; (e) a format-lint fixture set proving every divergence class above is normalized or rejected with an actionable message, wired into `verify.test` + `/sw-doc-review`. **Should close:** yes — standards MUST be deterministic across models or doc automation is non-reproducible. **Follow-up:** `/sw-brainstorm` → dedicated PRD (doc-format robustness / parser unification); cross-links `skills/spec-rigor`, `scripts/spec-union.sh`, `scripts/traceability-check.sh`, `scripts/wave_deliver.py`, and the living-doc reconcile family (GAP-043/GAP-044); Recallium learnings #2193 (D-ID extraction workaround), #2172 (gap-capture scope boundary), feature #2117 (Phase Dependencies validation). | open — captured 2026-06-27 via /sw-feedback; brainstorm→PRD not yet authored |
| GAP-046 | 2026-06-27 | /sw-doc PRD 024 A2 absorption + user review | 009 | **Gap absorption into a PRD/amendment does not mechanically flip backlog status (`open` → `planned`):** when a PRD or amendment absorbs a gap (adds requirements that close it / references `GAP-NNN` as motivation), nothing updates that gap's GAP-BACKLOG row from `open` to `planned — PRD <n>`. **Demonstrated this session:** GAP-039/GAP-040 were absorbed into **PRD 024 A2** (amendment frozen 2026-06-27, parent not-started) but stayed `open` until corrected by hand. This is the **upstream half** of GAP-043: GAP-043 covers the *downstream* `planned → resolved` flip on PRD **ship**; this row covers the *absorption-time* `open → planned` flip at PRD/amendment **freeze** — both are manual today and both silently misrepresent backlog state. **Proposed fix:** (1) absorption-time step in `/sw-freeze` (PRD + amendment): when a frozen artifact declares it closes a gap — preferably a machine-readable frontmatter key (`absorbs: [GAP-039, GAP-040]`) or the `Absorbed-by` column proposed in GAP-043, not prose scraping — flip each referenced row `open → planned — PRD <n>[ A<k>]` and refresh the index counts, under the living-doc lock / single-writer (reuse PRD 013 R10/R12 + PRD 022 R32 serialization); (2) `docs-currency-gate.sh` check that **fails closed** when a frozen PRD/amendment declares `absorbs: GAP-NNN` but the row is still `open` (and the converse: a `planned — PRD N` row whose PRD carries no matching reference); (3) compose with GAP-043 (`planned → resolved` on ship) and GAP-044 (stable IDs + index integrity) so the full lifecycle `open → planned → resolved` is mechanical end-to-end; (4) fixtures: freezing an amendment with `absorbs: GAP-NNN` flips the row + updates counts; a declared-but-unflipped row fails the gate. **Should close:** yes — without it, `open` gaps silently lie about backlog state (as GAP-039/GAP-040 just did before this manual fix). **Follow-up:** fold into GAP-043's owning PRD/amendment (PRD 009 living-status; A1 R49 extension) — one shared `gap-resolve` machinery, not a competing mechanism. | open — captured 2026-06-27; consolidate with GAP-043 (PRD 009 living-status), cross-links GAP-044 |
