# Gap backlog (living, append-only)

Committed trivial in-scope gaps written out-of-loop by `/pf-feedback` (Phase 2). Unlike frozen task
lists, this file is hand-appendable and not subject to the freeze CI check.

| Date | Source | PRD | Gap | Status |
|------|--------|-----|-----|--------|
| 2026-06-25 | /sw-deliver PRD 005 run | 004 | Pre-push secret scan: `sk_live_` fixture string reached `git push` and was caught only by GitHub push protection (last line, not first). Add a deny-pattern scan (`sk_(live\|test)_`, `ghp_`, PEM, etc.) to `/sw-stabilize` or terminal-PR prep so secrets fail locally before push. | resolved — PRD 007 R41/R50/R51 (`scripts/secret-scan.sh`), PR #67 |
| 2026-06-25 | /sw-deliver PRD 005 run | 004 | Scoped history-redaction guardrail: removing a secret from history must be range-scoped (`git filter-branch <base>..<branch>` or `git rebase -i`); a bare-branch `filter-branch` rewrote shared main commits under new SHAs and produced spurious PR conflicts. Add a `rules/` guardrail + doc note. | resolved — PRD 007 R42/R52 (`scripts/redaction-guard.sh` + `core/rules/sw-redaction-scope.mdc`), PR #67 |
| 2026-06-25 | /sw-deliver PRD 005 run | 004 | Phase `status.json` written under the phase worktree's `.cursor/` but `wave.sh status collect` reads the orchestrator-root path; required manual copy per phase. `status collect` should resolve the durable phase-worktree path directly (R38 intent). | resolved — PRD 007 R38, PR #67 |
| 2026-06-25 | /sw-deliver PRD 005 run | 004 | `merge run-next` assumes a per-phase PR (`gate-check` → "no open PR"); local phase-mode has no PRs, forcing manual `merge exec` fallback every phase. Add a no-PR local-merge path for phase-mode. | resolved — PRD 007 R39/R54, PR #67 |
| 2026-06-25 | /sw-deliver PRD 005 run | 004 | Orchestrator merges into its own worktree checkout; the primary checkout's `<type>/<slug>` ref needed manual `git merge --ff-only` after each phase. Deliver should advance/sync the primary ref automatically. | resolved — PRD 007 R40/R55, PR #67 |
| 2026-06-25 | PRD 009 deliver review (604abd4 revert) | 009 | **`core/scripts/` mirror drift:** runtime + fixtures use repo-root `scripts/` (`wave.sh` → `$ROOT/scripts/*`). `core/scripts/` is generated by `scripts/copy-to-core.sh` (PRD 004 — MUST NOT hand-edit). After PRD 009 phases the mirror is stale (~15 divergent files; missing `docs-presence-check.sh`, `docs-currency-gate.sh` hooks in `wave.sh`, etc.). Premature commit `604abd4` patched `core/scripts/` directly and deleted `model-routing.defaults.json`. **Follow-up:** run `bash scripts/copy-to-core.sh`, commit sync, and add a parity fixture (or wire into `run-emitter-fixtures.sh` / `verify.test`) so `core/scripts/` cannot drift from `scripts/` undetected. | resolved — `scripts/test/run-core-scripts-parity-fixtures.sh` + `copy-to-core.sh` sw-reference preserve (PRD 009 follow-up) |
| 2026-06-25 | /sw-doc PRD 003 run + user review | 005 | **`doc.afterTasks` guidance stale in `sw-doc.md`:** frozen amendments A1/A2 require `/sw-deliver run <frozen-task-list>` as the user-facing post-freeze entry (with `wave.sh spec-seed` on the boundary for `stop`/`confirm`/`auto`), but `core/commands/sw-doc.md` still prints raw `bash scripts/wave.sh deliver-loop` as the primary next command. **Follow-up:** align `sw-doc.md` (and dist emit) with A1 R76–R79 / A2 R80–R83; ensure `doc-afterTasks-*` fixtures pass; user guides already document `/sw-deliver run`. | resolved — PRD 011 R1–R4 (PR #76) |
| 2026-06-25 | /sw-doc PRD 003 run + user review | 002 | **`doc.afterTasks: confirm` checkpoint easy to miss:** with `doc.afterTasks: confirm`, `/sw-doc` emits the proceed/yes ask as prose at the end of a long pipeline summary (soft halt — no blocking UI). Unrelated follow-up messages (e.g. `/sw-memory-sync`, doc questions) do not re-prompt and correctly map to stop, but users may not realize implementation is waiting on an explicit ack. **Follow-up:** make the confirm boundary visually prominent in `sw-doc.md` output contract (dedicated section, direct question, halt without unrelated work); consider re-emitting checkpoint when user returns without ack; optional fixture for confirm prompt shape per PRD 002 R2. | resolved — PRD 011 R5–R7 (PR #76) |
| 2026-06-25 | /sw-doc PRD 010 run + user review | 008 | **Model tier routing not bound at Task dispatch (R9 procedural only):** `models.routing` maps `sw-doc-review` → `build` → `composer-2.5`, but persona subagents (`sw-*-reviewer`, `model: inherit`) ran on Haiku 4.5 because Cursor resolves `inherit` from the parent chat session — not `workflow.config.json`. PRD 008 scoped out hook-based Task injection and mandatory `resolve-model-tier.sh` at dispatch time; static CI cannot catch runtime drift. **Should close:** yes — tier policy is meaningless if subagents ignore it. **Can close (partial → full):** (1) **procedural** — harden `/sw-doc-review` + `sw-subagent-dispatch` to require `resolve-model-tier.sh --command sw-doc-review` and explicit Task `model:` from `models.tiers` before every persona spawn; R9 halt when parent &lt; `roles.builder`; fixtures for dispatch contract; (2) **mechanical** — explore Cursor pre-tool hook to inject `modelId` on Task calls targeting `sw-*-reviewer` / native panel agents (PRD 008 deferred); (3) **structural** — replace reviewer `model: inherit` with setup-stamped concrete IDs or platform-default `build` slug (loses per-repo tier edits unless re-seeded). Full closure likely needs (1)+(2). | planned — PRD 012 R1–R5 (frozen) |
| 2026-06-25 | PRD 001–008 deferred-item audit | 001 | **CI link-check automation (v1 non-goal):** PRD 001 deferred automated link verification; R16 still requires manual link checks for README + guides. **Follow-up:** add optional `scripts/docs-link-check.sh` (or lychee/markdown-link-check) wired into `verify.test` / a doctor step — not blocking ship, but closes the v1 manual-only debt. | resolved — PRD 011 R11–R13 (PR #76) |
| 2026-06-25 | PRD 001–008 deferred-item audit | 003 | **PRD 003 not started — deferred v2 surface entirely open:** INDEX `not-started`. Parent + A1 deferred items still unimplemented: `review.provider: pr-agent` adapter + `/sw-setup` workflow scaffold; PR-Agent CLI in `/sw-review` phase 2 (DL-13 — upstream uncommitted review); `issue_comment` interactive tools; `auto_improve` in scaffolded workflow; in-repo `.pr_agent.toml` / `configuration.toml` deep customization; org-level GitHub Environments / OIDC / `/sw-upstream` pin automation. PRD 005 native panel shipped as the phase-1 local path instead. | open |
| 2026-06-25 | PRD 001–008 deferred-item audit | 004 | **`/sw-deliver` v1 deferrals still open:** (1) cross-feature waves mixing phase-mode + multi-feature units in one plan; (2) automatic file-set edge inference when phase metadata is missing (sequential fallback only today); (3) rich live `living-status` per-phase dashboard (minimal `run.log` + terminal report shipped — R54); (4) contention serialization durable feedback into `/sw-tasks` re-run (DL-14 — runtime notice only). **Subsequently closed elsewhere (no gap):** `doc.afterTasks` → `/sw-deliver` dispatch (PRD 005 A1, PRD 007 R5); Owner A `spec-seed` at `deliver-loop` entry (`wave_deliver_loop.py`); shared branch resolver via `wave.sh preflight` + `wave_spec_seed.py`. | planned — PRD 013 R13–R16 (frozen) |
| 2026-06-25 | PRD 001–008 deferred-item audit | 005 | **Native review panel follow-on deferrals (post-PR #61):** PRD marked complete; explicit deferrals remain: (1) hybrid native-shell + pluggable-engine architecture (DL-2 — `ce-code-review` selectable only); (2) adaptive per-repo latency budgeting / auto-skip-low-risk heuristics (`--fast`/`--skip-local` escape hatch shipped per R54/DL-19); (3) stack-specific specialists (frontend-races, iOS, etc.) on evidence. | open |
| 2026-06-25 | PRD 001–008 deferred-item audit | 008 | **Model tier config deferrals (distinct from runtime R9 gap):** PRD 008 non-goals still open: (1) `models.platforms.*` dual-map in one config (Option B — rejected D1, deferred revisit); (2) `models.routing.agents` map — per-agent tiers remain procedural in `sw-subagent-dispatch.mdc` only; (3) user-interactive model picker in `/sw-setup` (defaults + manual edit only). Runtime hook / mandatory resolver invocation tracked separately in the 008 Task-dispatch row above. | partially planned — PRD 012 R6–R8 (`models.routing.agents`, frozen); items (1) platforms dual-map + (3) interactive picker remain deferred |
| 2026-06-25 | /sw-cleanup user review | 004 | **`/sw-cleanup` confirm step is a manual bash hand-off, not an agent-driven prompt:** after the dry-run report, the command tells the user to run `bash scripts/cleanup.sh --confirm --yes` themselves. Preferred UX: the agent asks the user to confirm (yes/proceed), then runs the apply step on their behalf — keeping the human gate but removing the manual terminal command. **Follow-up:** update `core/commands/sw-cleanup.md` so the confirm path is an agent prompt → agent runs `bash scripts/cleanup.sh --confirm --yes` (or `SW_CLEANUP_CONFIRM=1`) after explicit ack; preserve all fail-closed protections (unmerged/default/current branch, in-flight deliver run-state, no `rm -rf`); confirm only deletes the `wouldRemove` set the user reviewed; sync dist emit. | resolved — PRD 011 R8–R10 (PR #76) |
| 2026-06-25 | /sw-doc PRD 010 run + user review | — | **Frozen PRDs are not auto-committed to `main`, creating a data-loss window:** `/sw-freeze` stamps `frozen: true` in frontmatter but does not commit the artifact to any branch. The files exist only as untracked/modified working-tree entries until `spec-seed` eventually runs at `deliver-loop` entry — which may be hours or sessions later. An accidental `git checkout -f`, `git clean -fd`, or stash-drop destroys PRD Q&A that cannot be trivially regenerated. **Root cause:** `spec-seed` seeds onto the feature branch, not `main`; no step currently commits docs to `main`. **Proposed fix:** extend `/sw-freeze` (or add a post-freeze hook) to automatically commit the frozen artifact to the current default branch (`main`) immediately after stamping — docs-only commit, no implementation content. Constraints: must not commit brainstorm files (`docs/brainstorms/`) per `spec-seed` exclusion logic; must be idempotent (no-op if already committed); must not block or modify the freeze verdict. Frozen artifacts on `main` are safe: PRD folders are isolated per `<n>-<slug>`, feature branches inherit them without conflict, and `spec-seed` already handles the already-seeded case as a no-op. | planned — PRD 013 R1–R5 (frozen); resolved to commit onto `<type>/<slug>` (NOT `main`) to preserve PRD 005 A2 — see PRD 013 DL-1 |
| 2026-06-25 | /sw-doc PRD 010 run + user review | — | **`deliver-loop` supports only one concurrent PRD; parallel product-area work is blocked:** `wave_deliver_loop.py` uses a single repo-wide state file (`.cursor/sw-deliver-state.json`) and a single repo-wide orchestrator lock (`.cursor/sw-deliver.lock`). `assert_run_identity` rejects any `deliver-loop` call whose `--task-list` differs from the in-progress `source_task_list` (exit 20, `stale-state` halt); `lock acquire --nonblock` (O_CREAT|O_EXCL) fails if the lock file exists regardless of target branch. This forces strict sequential delivery: one PRD must fully merge before the next can start. Teams working on independent product areas (e.g. PRD 003 review adapter + PRD 010 memory provider) cannot implement them concurrently, even though the feature branches are fully orthogonal. **Proposed fix:** scope state and lock per target branch — e.g. `.cursor/sw-deliver-state.<slug>.json` and `.cursor/sw-deliver-<slug>.lock` — so independent orchestrators can run in parallel for disjoint feature branches. Requires auditing all state/lock readers to accept the scoped path (via `--task-list` resolution), updating `assert_run_identity` to scope identity checks, and adding a concurrent-run index so `/sw-cleanup` and `/sw-status` can enumerate all live runs. Living-doc contention (`INDEX.md`, `CHANGELOG.md`) remains a serialization concern within each wave and must still be handled by the contention model, not the lock. | planned — PRD 013 R6–R12 (frozen) |
