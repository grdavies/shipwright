---
description: Disable intra-phase Task dispatch during background phase sub-agent runs (R45).
alwaysApply: false
capability:
  version: 1
  triggers:
    - type: conductor_mode
      selectionFamily: subagent-dispatch
      equals: background_phase
  metadata:
    selectionFamily: subagent-dispatch
    dispatchPosture: inline
    intraphaseSubagentDispatch: disabled
---

# Dispatch background-phase posture

Background phase sub-agents disable intra-phase Task dispatch — inline two-stage review only (R45).
