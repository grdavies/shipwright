[pytest]
testpaths = scripts/unit_tests tests scripts/test/host_switching
pythonpath = scripts
markers =
    integration: tests requiring integration fixtures or multi-component setup
    git: tests that mutate or inspect a real git repository
    slow: tests that exceed fast-scope time budget
filterwarnings =
    error
